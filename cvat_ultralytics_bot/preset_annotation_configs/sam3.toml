tool = "sam3"

[sam3]
# SAM3 model from ModelScope/Transformers
# Model ID: facebook/sam3
weights = "facebook/sam3"
conf = 0.25
device = "cpu"
# 是否使用多边形（实例分割），设为 false 则只输出 bounding box
use_polygon = true

[sam3.label_prompts]
person = "person"
vehicle = "vehicle"
